"""Capaciteitstarief (BE) — Home Assistant integration.

M1: only the HA-independent calculation core lives in ``core/``.
The HA wrapper (config flow, coordinator, entities, storage) follows in M2.
"""
